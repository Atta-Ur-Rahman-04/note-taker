<nav class="navbar navbar-expand-md navbar-custom bottom-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/notes"><i class="fa-solid fa-clipboard"></i></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <% if(currUser) {%>
          <a class="nav-link" href="/notes">All Notes</a>
          <a class="nav-link" href="/notes/new">Create <i class="fa-solid fa-notes-medical"></i></a>
          <% } %>
      </div>

      <% if(currUser) {%>
      <form action="/notes/search" method="GET" class="d-flex search-form">
        <input type="text" name="q" class="search-input" placeholder="Search your notes..."
          value="<%= typeof query !== 'undefined' ? query : '' %>" />
        <button type="submit" class="search-btn">
          <i class="fa-solid fa-magnifying-glass"></i>
        </button>
      </form>
      <% } %>
      <!-- The logic is explained in another folder -->

      <div class="navbar-nav ms-auto">
        <% if(!currUser) {%>
          <a class="nav-link" href="/login">LogIn</a>
          <a class="nav-link" href="#">|</a>
          <a class="nav-link" href="/signup">SignUp</a>
          <% } %>

            <% if(currUser) {%>
              <a class="nav-link" href="#"><i class="fa-solid fa-circle-user"></i>
                <%= currUser.username %>
              </a>
              <a class="nav-link" href="#"> | </a>
              <a class="nav-link" href="/logout">LogOut</a>
              <% } %>
      </div>
    </div>
  </div>
</nav>