<% layout("/layouts/boilerplate") %>
<div class="row mt-3">
  <h1 class="col-6 offset-3">Login to NoteTaker</h1>
  <div class="col-6 offset-3">
    <form
      action="/login"
      method="post"
      class="needs-validation mt-3"
      novalidate
    >
      <div class="mb-3">
        <label for="username" class="form-label">Username :</label>
        <input
          id="username"
          type="text"
          placeholder="Enter Your Username..."
          name="username"
          class="form-control"
          required
        />
        <div class="invalid-feedback">Enter a valid Username!</div>
      </div>
      <div class="mb-3">
        <label for="password" class="form-label">Password :</label>
        <input
          id="password"
          type="password"
          placeholder="Enter Your Password..."
          name="password"
          class="form-control"
          required
        />
        <div class="invalid-feedback">Enter a Strong Password!</div>
      </div>
      <Button class="btn signup-btn">Login</Button>
    </form>
  </div>
</div>
